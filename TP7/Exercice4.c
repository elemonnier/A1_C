#include<stdio.h>
#include<stdlib.h>
#include<string.h>

void Palindrome ()
{

}

int main (char argc, char** argv)
{
        if (argc == 0)
        {
                printf("Entrez une chaîne : ");
                fgets(chaine, 32, stdin);
                Palindrome(chaine);

                return 0;
        }
        if (argc == 1)
        {

        }
}
