#include <stdio.h>
#include <stdlib.h>

int main()
{
        int a, b, c;
        printf("Entrez la somme > ");
        scanf("%d\n", &a);
        printf("%d", &a);
        printf(" = ");
        if (a >= 100)
        {
                (b=a-100);
        }
        else
        {
                 (b=a);
        }
        if (b >= 50)
        {
                (c=b-50);
        }
        else
        {
                (c=b);
        }
        if (c >= 20)
        {
                (d=c-20);
        }
        else
        {
                (d=c);
        }
        if (d >= 10)
        {
                (e=d-10);
        }
        else
        {
                (e=d);
        }
        if (e >= 5)
        {
                (f=e-5)
        }
        else
        {
                (f=e)
        }
}
